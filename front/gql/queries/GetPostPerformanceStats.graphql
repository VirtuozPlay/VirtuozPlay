query GetPostPerformanceStats($id: ID!) {
  performance(id: $id) {
    id
    song {
      id
      title
    }
    author {
      id
      name
    }
    duration
    notes {
      at
      precision
    }
  }
}
